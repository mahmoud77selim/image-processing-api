<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Image processing API - Resize</title>
    <link href="../public/styles.css" rel="stylesheet" />
  </head>
  <body>

    <div class="topnav">
      <a href="/">Home Page</a>
      <a href="/resize?w=400&h=400"> Resized images 400*400</a>
    </div>

    <% if ( noImagesError ) { %>
    <div class="noParameters"><p>No image to resize</p></div>

    <% } else if ( noParameters ) { %>
    <div class="noParameters">
      <h3>
        <marquee
          >Size parameters is mandatory, kindy enter the width and height in the
          Page URL! <br />Example:
          http://localhost:8080/resize?w=400&h=400</marquee
        >
      </h3>
    </div>

    <% } else { %>
    <div class="info"><h2 class="">
        viewing images with the dimensions of: <% if ( width && height ) { %>
        <div>
          <span class="provided-parameters">Width</span>: <%= width %>,
          <span class="provided-parameters">Height</span>: <%= height %>
        </div>

        <% } else if (width) { %>
        <div><span class="provided-parameters">Width</span>: <%= width %></div>

        <% } else if (height) { %>
        <div><span class="provided-parameters">Height</span>: <%= height %></div>
        <% } %>
      </h2></div>
    <% } %>

    <div id="image-container">
      <% if ( data.length > 0 ) { %><% data.forEach((img) => { %>
      <img class="resized-image" src="public/thumbs/<%= img %>" /><%
      }); %><% } %>
    </div>

    <div class="footer">Copyright ©2022. All rights reserved.</div>
  </body>
</html>
